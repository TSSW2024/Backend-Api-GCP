apiVersion: apps/v1
kind: Deployment
metadata:
  name: api-binance-v2
spec:
  replicas: 1
  selector:
    matchLabels:
      app: api-binance-v2
  template:
    metadata:
      labels:
        app: api-binance-v2
    spec:
      containers:
      - name: api-binance-v2
        image: {IMAGE_TAG}
        ports:
        - containerPort: 9090
        resources:
          # You must specify requests for CPU to autoscale
          # based on CPU utilization
          limits:
            cpu: 50m
            memory: 128Mi
          requests:
            cpu: 50m
            memory: 128Mi