apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: api-binance-ingress
  namespace: default
  annotations:
    kubernetes.io/ingress.class: nginx
    cert-manager.io/cluster-issuer: letsencrypt-prod
    acme.cert-manager.io/http01-edit-in-place: "true"
spec: 
  rules:
  - host: api-binance.tssw.cl
    http:
      paths:
      - backend:
          service:
            name: api-binance-ingress
            port:
              number: 80
        pathType: Prefix
        path: /
  tls:
  - hosts:
    - api-binance.tssw.cl
    secretName: api-binance-ingress-secret