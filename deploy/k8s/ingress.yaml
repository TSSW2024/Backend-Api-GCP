apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: api-binance-ingress-v2
  namespace: default
  annotations:
    kubernetes.io/ingress.class: nginx
    cert-manager.io/cluster-issuer: letsencrypt-prod
    acme.cert-manager.io/http01-edit-in-place: "true"
spec: 
  rules:
  - host: api-binance-v2.tssw.cl
    http:
      paths:
      - backend:
          service:
            name: api-binance
            port:
              number: 80
        pathType: Prefix
        path: /
  tls:
  - hosts:
    - api-binance-v2.tssw.cl
    secretName: api-binance-ingress-v2-secret